# Required gem for Windows
gem 'wdm', '>= 0.1.0' if Gem.win_platform?

# Required for github pages
source 'https://rubygems.org'
gem 'github-pages'

# Required for SyntaxHighlightFilter
# gem 'github-linguist'